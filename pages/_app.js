/**
 * Next.js App Component
 * Global layout and configuration
 *
 * To be extended in Phase 2 with design system and component library
 */

export default function App({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
